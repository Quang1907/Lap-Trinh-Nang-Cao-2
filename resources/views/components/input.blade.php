<div class="form-group">
    <label for="{{ $id }}">{{ $name }}</label>
    <input type="{{ $type }}" class="form-control" id="{{ $id }}" name="{{ $id }}"
        placeholder="{{ $place }}" value="{{ $value }}">
</div>
